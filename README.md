# PHP User Authentication System (MySQL + Bootstrap 5)

**A simple, secure PHP-based user authentication system** using MySQL.  
Features: user registration with password hashing, login + session management, protected dashboard, and responsive UI built with Bootstrap 5.

**Topics / Tags:** `PHP`, `MySQL`, `Bootstrap`, `Authentication`, `Login System`, `Signup System`, `Secure Passwords`

---

## 🔧 Features
- ✅ User registration with validation  
- 🔒 Passwords stored securely using `password_hash()`  
- 🔑 Login with session-based authentication (`$_SESSION`)  
- 🚫 Protected dashboard/welcome page (redirects if not logged in)  
- 📱 Responsive UI using Bootstrap 5  
- 🧾 Simple and clear code — easy to customize
